__pycache__/
*.pyc
.venv/
venv/

# Local secrets - never commit real store credentials
.env
.env.*
!.env.example

# Local data exports - business/customer data, not source code.
# The fetched payload and the rendered dashboard both embed customer names,
# emails and order numbers, so neither belongs in git.
*.csv
data/
dist/
